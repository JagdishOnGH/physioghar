enum SlotStatus { open, booked, blocked }

enum RequestStatus { pending, accepted, declined }

enum BookingTab { requests, upcoming, completed, cancelled }

enum ComplaintCategory { appBug, schedulingIssue, paymentInquiry, patientBehavior, other }
